## ✅ Step 1: Install Git (Skip if already installed)

1. Go to [https://git-scm.com/downloads](https://git-scm.com/downloads)
2. OR in the terminal run this
3. winget install --id Git.Git -e --source winget
4. Download the **Latest source Release** for your operating system.
5. Install it using default settings
6. Open a terminal and check installation:
   ```bash
   git -v
   ```
7. To Clone the Repository -- Go to the folder in which you want to download the code (class-code) folder.
   ```bash
   git clone https://github.com/msadeedashraf/react-052025-am.git
   ```
8. To Fetch the code after new updates execute the fetch and pull.
   ```bash
   git fetch
   git pull
   ```